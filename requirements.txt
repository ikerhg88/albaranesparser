pandas>=2.0
numpy>=1.24
pdfplumber>=0.10
XlsxWriter>=3.1
Pillow>=10.0
pypdfium2>=4.20
opencv-python-headless>=4.8

# OCR soportado en produccion:
# - Tesseract como binario externo integrado o instalado en PATH.
#
# OCRmyPDF y Doctr se mantienen solo como motores experimentales opcionales.
# No deben instalarse como dependencia base porque arrastran binarios/modelos
# pesados y no son necesarios para la configuracion validada.
